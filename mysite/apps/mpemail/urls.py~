from django.conf.urls import url, include

urlpatterns = [
]


from email.rest.viewsets.email import EmailViewSet
router.register(r'email', EmailViewSet)

urlpatterns += [
    url(r'^rest_api/', include(router.urls)),
]
